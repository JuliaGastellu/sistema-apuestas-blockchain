const config = {
    // The URL of the Flask prediction API.
    // Make sure the Flask server (app.py) is running and accessible at this address.
    API_URL: 'http://127.0.0.1:5000'
};

export default config;
